import { n as useIndustry } from "./IndustryContext-BipHHbci.js";
import { createContext, useCallback, useContext, useEffect, useMemo, useState } from "react";
import { jsx } from "react/jsx-runtime";
//#region src/data/dashboardModules.ts
var DASHBOARD_MODULES = [
	{
		id: "hero",
		label: "Today's pulse",
		description: "Executive summary for the day",
		industries: "all",
		pinned: true
	},
	{
		id: "industry-kpis",
		label: "Industry KPIs",
		description: "Operating metrics tuned to your workspace",
		industries: "all"
	},
	{
		id: "industry-modules",
		label: "Industry modules",
		description: "Workflows specific to this industry",
		industries: [
			"accounting",
			"real-estate",
			"professional-services",
			"healthcare",
			"ecommerce"
		]
	},
	{
		id: "industry-playbooks",
		label: "Recommended plays",
		description: "Tailored recommendations",
		industries: "all"
	},
	{
		id: "connectors",
		label: "Connectors",
		description: "Quick access to data sources",
		industries: "all"
	},
	{
		id: "briefing",
		label: "Daily briefing",
		description: "Narrative morning briefing",
		industries: "all"
	},
	{
		id: "actions",
		label: "Action register",
		description: "Open actions across the team",
		industries: "all"
	},
	{
		id: "followups",
		label: "Follow-up autopilot",
		description: "Drafted follow-ups awaiting approval",
		industries: [
			"sales",
			"professional-services",
			"real-estate"
		]
	},
	{
		id: "meetings",
		label: "Meeting to execution",
		description: "Meeting outcomes converted to work",
		industries: "all"
	},
	{
		id: "kpi",
		label: "KPI digest",
		description: "Executive KPI snapshot",
		industries: "all"
	},
	{
		id: "forecast",
		label: "Revenue forecast",
		description: "Quarterly forecast with commit bands",
		industries: [
			"sales",
			"accounting",
			"professional-services",
			"ecommerce"
		]
	},
	{
		id: "pipeline-risk",
		label: "Pipeline risk radar",
		description: "Deals with slipping signals",
		industries: [
			"sales",
			"real-estate",
			"professional-services"
		]
	},
	{
		id: "deals",
		label: "Deal progression",
		description: "Stage movement this week",
		industries: ["sales", "real-estate"]
	},
	{
		id: "health",
		label: "Account health",
		description: "Customer health scoring",
		industries: [
			"sales",
			"professional-services",
			"healthcare"
		]
	},
	{
		id: "sentiment",
		label: "Email sentiment",
		description: "Signals across customer threads",
		industries: [
			"sales",
			"professional-services",
			"healthcare"
		]
	},
	{
		id: "recommendations",
		label: "Recommendations",
		description: "AI-suggested next moves",
		industries: "all"
	},
	{
		id: "renewals",
		label: "Renewal reminders",
		description: "Upcoming contract renewals",
		industries: [
			"sales",
			"real-estate",
			"professional-services"
		]
	},
	{
		id: "timeline",
		label: "Communication timeline",
		description: "Recent stakeholder touches",
		industries: "all"
	},
	{
		id: "approvals",
		label: "Approvals",
		description: "Items awaiting sign-off",
		industries: "all"
	},
	{
		id: "weekly",
		label: "Weekly performance",
		description: "Rolling weekly scorecard",
		industries: "all"
	},
	{
		id: "readiness",
		label: "Product readiness",
		description: "Launch and rollout readiness",
		industries: [
			"sales",
			"ecommerce",
			"professional-services"
		]
	}
];
function modulesForIndustry(id) {
	return DASHBOARD_MODULES.filter((m) => m.industries === "all" || m.industries.includes(id));
}
function defaultOrderFor(id) {
	return modulesForIndustry(id).map((m) => m.id);
}
//#endregion
//#region src/context/DashboardLayoutContext.tsx
var LayoutCtx = createContext(null);
var STORAGE_KEY = "lucent.layout.v1";
function loadMap() {
	try {
		const raw = localStorage.getItem(STORAGE_KEY);
		if (!raw) return {};
		return JSON.parse(raw);
	} catch {
		return {};
	}
}
function saveMap(map) {
	try {
		localStorage.setItem(STORAGE_KEY, JSON.stringify(map));
	} catch {}
}
/** Reconcile a stored layout with the registry:
*  - drop ids that no longer exist for this industry
*  - append any new registry ids to the end
*  - keep pinned modules always first and visible
*/
function reconcile(industryId, stored) {
	const available = modulesForIndustry(industryId);
	const availableIds = new Set(available.map((m) => m.id));
	const pinnedIds = available.filter((m) => m.pinned).map((m) => m.id);
	const baseOrder = stored?.order?.filter((id) => availableIds.has(id)) ?? [];
	const missing = available.map((m) => m.id).filter((id) => !baseOrder.includes(id));
	let order = [...baseOrder, ...missing];
	order = [...pinnedIds, ...order.filter((id) => !pinnedIds.includes(id))];
	const hidden = (stored?.hidden ?? []).filter((id) => availableIds.has(id) && !pinnedIds.includes(id));
	return {
		order,
		hidden
	};
}
function DashboardLayoutProvider({ children }) {
	const { industryId } = useIndustry();
	const [map, setMap] = useState({});
	useEffect(() => {
		setMap(loadMap());
	}, []);
	const layout = useMemo(() => reconcile(industryId, map[industryId]), [industryId, map]);
	const persist = useCallback((next) => {
		setMap((prev) => {
			const updated = {
				...prev,
				[industryId]: next
			};
			saveMap(updated);
			return updated;
		});
	}, [industryId]);
	const toggle = useCallback((id) => {
		const mod = DASHBOARD_MODULES.find((m) => m.id === id);
		if (!mod || mod.pinned) return;
		const isHidden = layout.hidden.includes(id);
		persist({
			order: layout.order,
			hidden: isHidden ? layout.hidden.filter((x) => x !== id) : [...layout.hidden, id]
		});
	}, [layout, persist]);
	const reorder = useCallback((from, to) => {
		if (from === to) return;
		const next = [...layout.order];
		const [moved] = next.splice(from, 1);
		if (!moved) return;
		if (DASHBOARD_MODULES.find((m) => m.id === moved)?.pinned) return;
		const pinnedCount = layout.order.filter((id) => DASHBOARD_MODULES.find((m) => m.id === id)?.pinned).length;
		const clampedTo = Math.max(pinnedCount, Math.min(to, next.length));
		next.splice(clampedTo, 0, moved);
		persist({
			order: next,
			hidden: layout.hidden
		});
	}, [layout, persist]);
	const reset = useCallback(() => {
		persist({
			order: defaultOrderFor(industryId),
			hidden: []
		});
	}, [industryId, persist]);
	const value = useMemo(() => {
		const hiddenSet = new Set(layout.hidden);
		return {
			modules: modulesForIndustry(industryId),
			orderedIds: layout.order.filter((id) => !hiddenSet.has(id)),
			allOrderedIds: layout.order,
			isVisible: (id) => !hiddenSet.has(id),
			toggle,
			reorder,
			reset
		};
	}, [
		industryId,
		layout,
		toggle,
		reorder,
		reset
	]);
	return /* @__PURE__ */ jsx(LayoutCtx.Provider, {
		value,
		children
	});
}
function useDashboardLayout() {
	const ctx = useContext(LayoutCtx);
	if (!ctx) throw new Error("useDashboardLayout must be used inside DashboardLayoutProvider");
	return ctx;
}
//#endregion
export { useDashboardLayout as n, DASHBOARD_MODULES as r, DashboardLayoutProvider as t };
